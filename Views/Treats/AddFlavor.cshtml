@model TreatsTracker.Models.Treat

<h2>Add a Treat</h2>

@if (ViewBag.TreatCount == 0)
{
  <h4>You have not added any treats yet! Please add them <span>@Html.ActionLink("here", "Index", "Treats")</span>
</h4>
}
else
{
  <h4>Assign a flavor to this treat: @Html.DisplayFor(model => model.Name)</h4>
  @using (Html.BeginForm())
  {
    @Html.HiddenFor(model => model.TreatId)

    @Html.Label("Select item")
    @Html.DropDownList("FlavorId")

    <input type="submit" value="Save" />
  }
}

<p>@Html.ActionLink("Back to list", "Index")</p>